import { OrderByViewsPipe } from './order-by-views.pipe';

describe('OrderByViewsPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByViewsPipe();
    expect(pipe).toBeTruthy();
  });
});
